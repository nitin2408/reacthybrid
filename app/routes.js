import React from 'react';
import ReactDOM from 'react-dom';
import { Router, Route, hashHistory,IndexRoute,IndexRedirect,DefaultRoute,Redirect,BrowserHistory } from 'react-router';
import App from './components/app.js';

export default (
            <Route path = "/" component = {App}></Route>
)
